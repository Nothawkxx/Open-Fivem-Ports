# Open-Fivem-Ports
Simple batch script to open required VPS ports for FiveM servers. Run as Administrator to automatically allow port 30120 (CFX) and 40120 (txAdmin) through Windows Firewall. Quick and easy setup for getting your server online without manual firewall configuration.
